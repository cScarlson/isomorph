// app.directive('partialStore', function(/* dependencies */){
	// return {
		// restrict: 'E',
		// scope: {exp: '='}
		// llink: function(scope, element, attrs){
			// scope.$watch(function(newVal, oldVal){
				// console.log('newVal ' + newVal + ' oldVal ' + oldVal);
			// });
		// }
	// };
// });



// app.directive('dirname', function factory(injectables){
	// var dirDefObj = {
		// priority: 0,
		// template: '<div>This is my template!</div>',
		// replace: false,
		// restrict: 'A',
		// scope: false,
		// compile: function preLink(tElement, tAttrs, transclude){}
	// };
	// return dirDefObj;
// });

// app.directive('mydir', function(){
	// return {
		// restrict: 'E',
		// scope: {exp="1"},
		// link: function(scope, element, attrs){
			// scope.$watch(function(newVal, oldVal){
				// newVal += 4;
			// });
		// }
	// };
// });