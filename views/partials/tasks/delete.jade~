extends ../layout

block content
	h1= title
	mark Are you sure you want to delete #{doc.task}?
	br
	b #{doc.id}
	br
	form(method="post", action="#/tasks/#{doc.id}")
		input(type="hidden", name="_method", value="delete")
		input(type="hidden", name="task[task]", value="#{doc.id}")
		input(type="submit", value="YES, DELETE")
	br
	a(href="#/tasks")
		button CANCEL
